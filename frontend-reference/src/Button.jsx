import React from "react";
import { Button, borders } from "@mui/material";

const App = () => {

    return (

        <Button style={{color: 'orange'}} sx={{ borderRadius: 2 }} variant="contained">Generate Bill</Button>

    );

}